// in this class I have saved the colors as variables, so it is easier to use later
import 'package:flutter/material.dart';

Color background = const Color.fromARGB(255, 255, 232, 182);
Color text =
    background; // added this variable for an easier implementation later
Color altText = const Color.fromARGB(255, 255, 172, 0);
Color lightWidget = Color.fromARGB(160, 166, 173, 90);
Color darkWidget = Color.fromARGB(150, 119, 141, 69);
